Flask==3.0.3
# Pillow 12.x supports Python 3.11-3.14 and publishes binary wheels for supported platforms.
# Keep this major-version bounded; validate the next Pillow/Python generation before widening it.
Pillow>=12.0.0,<13
python-dateutil==2.9.0.post0
requests>=2.31.0
